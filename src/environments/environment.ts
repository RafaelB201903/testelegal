// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: true,
  firebase : {
    apiKey: "AIzaSyBoxgGB2XMj8oKNSx2Fy_RExBdtCzzf_1M",
    authDomain: "proj-final-lightpet.firebaseapp.com",
    databaseURL: "https://proj-final-lightpet.firebaseio.com",
    projectId: "proj-final-lightpet",
    storageBucket: "proj-final-lightpet.appspot.com",
    messagingSenderId: "657124352584",
    appId: "1:657124352584:web:d146864f427ec90a8b2e7d",
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
